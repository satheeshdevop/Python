# Multiply all numbers in the list
 # Sample list of numbers

numbers = [10, 20, 30, 40, 50]
# Initialize a variable to store the product

product_of_numbers = 1
# Iterate through the list and accumulate the product

for i in numbers:
   product_of_numbers *= i
          # Print the product
   print("Product of elements in the list:", product_of_numbers)
# Sample list of numbers
numbers = [30, 10, -45, 5, 20]
# Initialize a variable to store the minimum value, initially set to to Multiply all numbers in the list